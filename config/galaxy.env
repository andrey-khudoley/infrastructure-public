# ---------------------------------------------------------------------------
# Ansible Galaxy / make install-deps в приватном репо (шаг 70)
# ---------------------------------------------------------------------------
GALAXY_INSTALL_TIMEOUT=300
GALAXY_DOWNLOAD_DIR=/var/lib/infra/galaxy-download
GALAXY_INSTALL_RETRIES=5
GALAXY_RETRY_SLEEP_SEC=10
# COLLECTIONS_REQ=   # опционально; иначе по умолчанию PULL_DIR/collections/requirements.yml
